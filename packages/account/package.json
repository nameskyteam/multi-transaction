{
  "name": "@multi-transaction/account",
  "version": "2.1.0",
  "license": "MIT",
  "description": "Multi Transaction Account implementation.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "commonjs",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "check": "tsc --noEmit",
    "lint": "eslint 'src/**/*.ts' --fix",
    "prettier": "prettier 'src/**/*.ts' --write --list-different"
  },
  "dependencies": {
    "@multi-transaction/common-utils": "workspace:*",
    "@multi-transaction/core": "workspace:*",
    "@near-js/accounts": "=2.3.0",
    "@near-js/providers": "=2.3.0",
    "@near-js/signers": "=2.3.0",
    "@near-js/types": "=2.3.0",
    "buffer": "^6.0.3"
  }
}
